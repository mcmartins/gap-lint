{
  "rules": [
    {
      "name": "IF",
      "when": {
        "regex": "/\bif\b/"
      },
      "then": [
        {
          "regex": "/\bthen\b(.*)/",
          "inline": false
        },
        {
          "regex": "/\bfi;(.*)/",
          "inline": false
        }
      ]
    }
  ]
}